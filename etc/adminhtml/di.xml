<?xml version="1.0"?>
<!--
/**
 * Etechflow_AbandonedCart - Admin-scope DI.
 *
 * NOTE: The UI Component grid data-source registrations
 * (Magento\Framework\View\Element\UiComponent\DataProvider\CollectionFactory
 * `collections` array) were moved OUT of this admin scope and into the global
 * etc/di.xml.
 *
 * Reason: the `collections` array is compiled per-scope. Declared in the
 * adminhtml scope, Magento REPLACES the merged array instead of merging it,
 * which wiped the ~88 core grid registrations (sales_order_grid_data_source,
 * customer_listing_data_source, cms_page_listing_data_source, etc.) and broke
 * every other admin grid once this module was installed.
 *
 * Rule of thumb: grid data-source registrations belong in GLOBAL etc/di.xml,
 * never in etc/adminhtml/di.xml.
 *
 * @category   ETechFlow
 * @package    Etechflow_AbandonedCart
 */
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">

</config>
